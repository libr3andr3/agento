<?xml version="1.0" encoding="utf-8"?>
<!-- Camera capture target for the catalog-photo flow (cacheDir) and the
     downloaded-update APK handed to the package installer (external files). -->
<paths>
    <cache-path name="camera" path="." />
    <external-files-path name="updates" path="updates/" />
</paths>
